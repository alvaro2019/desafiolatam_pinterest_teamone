<nav class="navbar navbar-expand-md bg-dark navbar-dark">
  <%= link_to pins_path do %> 
  <!-- LOGO CENTRAL -->
    <%= image_tag asset_path('logo_pinterest.png'), class: 'img-custom' %>
  <% end %>

  <!-- MOBILE : esto para funcione el resposivo y se combierta en boton cuando sea pequeña la pantalla-->
  <!-- <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button> -->

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
    <span class="navbar-toggler-icon"></span>
  </button>
  

  <div class="collapse navbar-collapse" id="collapsibleNavbar" id="navbarcentral">

    <!-- FORMULARIO DE BUSQUEDA --> 
    <form class="form-inline my-3 my-lg-0 menubarra" >
      <input class="form-control menubarra"  type="search" placeholder="Buscar" aria-label="Search">
    </form>

    <ul class="nav navbar-nav navbar-right">

      <% if current_user.nil? %>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_session_path %>">Ingreso</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_registration_path %>">Registro</a>
        </li>
      <% else %>
      <%= current_user.email %>
      <%= image_tag current_user.avatar, class: 'img-fluid img-custom img-circle' %><!-- img-custom -->
        <li class="nav-item">
          <a class="nav-link" href="<%= new_pin_path %>">Nuevo Pin</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= destroy_user_session_path %>" data-method="delete">Salir</a>
        </li>  
      <% end %> 
    </ul>
  </div> 
</nav>
